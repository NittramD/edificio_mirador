# Puerto en el que corre el backend (Express)
PORT=4000

# String de conexión a la base de datos PostgreSQL (Neon u otro proveedor)
# Crea un proyecto gratis en https://neon.tech/ y copia el "Connection string"
DATABASE_URL=postgresql://usuario:password@host/nombre_db?sslmode=require
